---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jitsi-meet
  namespace: jitsi-meet
spec:
  interval: 15m
  chart:
    spec:
      chart: jitsi-meet
      version: 2.16.0
      sourceRef:
        kind: HelmRepository
        name: jitsi-contrib
        namespace: flux-system
      interval: 15m
  timeout: 20m
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  # NOTE: key names below follow the jitsi-contrib/jitsi-helm values.yaml schema,
  # not truecharts/app-template. Verify against:
  # https://github.com/jitsi-contrib/jitsi-helm/blob/main/values.yaml
  values:
    publicURL: https://talk.${DOMAIN_1}
    tz: America/Chicago
    enableAuth: false
    enableGuests: true

    web:
      replicaCount: 1
      ingress:
        enabled: true
        ingressClassName: external
        annotations:
          cert-manager.io/cluster-issuer: domain-0-le-prod
          nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"
          nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
          nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
        hosts:
          - host: talk.${DOMAIN_1}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: jitsi-meet-web-tls
            hosts:
              - talk.${DOMAIN_1}

    prosody:
      replicaCount: 1

    jicofo:
      replicaCount: 1

    # JVB handles media (UDP/10000). Must stay at replicaCount: 1 unless
    # OCTO relay is enabled. MetalLB assigns ${JITSI_IP} for the UDP service.
    jvb:
      replicaCount: 1
      service:
        type: LoadBalancer
        loadBalancerIP: ${JITSI_IP}
        port: 10000
        protocol: UDP
